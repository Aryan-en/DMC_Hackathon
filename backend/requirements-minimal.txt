# ONTORA Backend - MINIMAL WINDOWS PYTHON 3.13 (PRE-BUILT WHEELS ONLY)
# Use: pip install -r requirements-minimal.txt --only-binary :all:
# This is the most stable configuration - add more packages only as needed

# Web Framework & Async (NO BUILD NEEDED)
fastapi==0.128.3
uvicorn[standard]==0.40.0
pydantic==2.12.5
pydantic-settings==2.13.1

# Database & ORM (NO BUILD NEEDED - asyncpg handles async DB)
sqlalchemy==2.0.48
asyncpg==0.31.0
neo4j==6.1.0
redis==7.3.0

# HTTP & Web (NO BUILD NEEDED)
requests==2.32.5
httpx==0.28.1
beautifulsoup4==4.13.5

# Messaging (NO BUILD NEEDED)
kafka-python==2.0.2

# Data Processing (Optional - omit if not needed)
# Note: numpy pinned to <2.3.0 for opencv-python compatibility
numpy>=2.0,<2.3.0
pandas>=2.1.0

# Utilities & Security (NO BUILD NEEDED)
python-dotenv==1.2.1
python-multipart==0.0.22
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0

# Monitoring (NO BUILD NEEDED)
prometheus-client==0.23.1
python-json-logger==3.3.0
